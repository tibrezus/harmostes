name: Generate RIG

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  rig:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate RIG
        uses: tibrezus/llm-wiki/.github/actions/repo-map@2ebca7840afbb1850731f4d45fb47a97e5c7faf7
        with:
          language: go
          focus-package-prefixes: |
            github.com/tibrezus/harmostes/cmd
            github.com/tibrezus/harmostes/internal

      - name: Upload RIG to release
        uses: softprops/action-gh-release@v2
        with:
          files: repo-map.json
          fail_on_unmatched_files: true